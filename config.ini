# 标记 Git 跳过这个文件的工作区修改
# 在项目目录用Git Bash执行：
# git update-index --assume-unchanged config.ini
# git update-index --skip-worktree config.ini
# 取消跳过：
# git update-index --no-assume-unchanged config.ini
# git update-index --no-skip-worktree config.ini

;========================= - Setting - =========================;
[SHORTCUTS]
    # PATH
    dspaz = explore "D:\Projects\AutoHotKey"
    dnets = explore "D:\Desktop-Toolbar\Network"
    dToolbar = explore "D:\Desktop-Toolbar"
    download = explore "E:\Downloads"
    
    StartUp = shell:startup
    # Game AHK

    # Games
    
    Steam = ""

    # Tools
    
    # Others
    config = config.ini

[AKA]
    sps = Steam
    qidong = StartUp
    Desktop-Toolbar = dToolbar
    DT = dToolbar
    Toolbar = dToolbar
    spaz = dspaz

[Switches]
    VIM = 1
